server:
  port: 8085

spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:mysql://localhost:3306/homefix_booking
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
